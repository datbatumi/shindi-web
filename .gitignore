# WordPress backend — managed separately, not part of the static frontend repo
sadmin/

# Claude Code local config — never deploy these
.claude/

# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo

# Sensitive / local-only files
pass - forms.txt
*.env
.env*
